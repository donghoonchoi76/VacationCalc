<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="VacationCalculator.Views.SettingsPage"
             xmlns:vm="clr-namespace:VacationCalculator.ViewModels"
             Title="{Binding Title}">

    <ContentPage.BindingContext>
        <vm:SettingsViewModel />
    </ContentPage.BindingContext>
    
    <ContentPage.Content>
        <StackLayout Spacing="20" Padding="15">
            <Label Text="Beginning Date" FontSize="Medium" />
            <DatePicker Date="{Binding BeginningDate}" FontSize="Small" />
            <Label Text="Initial Vacation Days" FontSize="Medium" />
            <Editor Text="{Binding InitialDays}" d:Text="Vacation description" Keyboard="Numeric" FontSize="Small" Margin="0" />
            <Label Text="Percent of Vacation Pay" FontSize="Medium" />
            <Editor Text="{Binding PercentOfPay}" d:Text="Vacation description" Keyboard="Numeric" FontSize="Small" Margin="0" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>